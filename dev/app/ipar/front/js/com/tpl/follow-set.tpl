<div class='follower'>
{{user_avt}}
<span class="in-line">
    <span class="nick">{{user_nick}}</span>
    <span  class="panel-follow-status {{is_following}}" data-status="{{is_following}}" data-uid="{{uid}}" style="float: right; margin: 2px;">
        <button class="button">
            <span class="followed">{{trans_followed_user}}</span>
            <span class="unfollowing">{{trans_cancel_follow}}</span>
            <span class="following">{{trans_following_user}}</span>
        </button>
    </span>
</span>
</div>