project.name = entity
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[eid]
type = id

[type_id]
index = self
tokenizer = full

[zcode]
index = self
tokenizer = full

[owner_uid]
index = self
tokenizer = full

[uid]
index = self
tokenizer = full

[title]
type = title

[content]
type = body

[rank]
type = numeric

[status]
index = self
tokenizer = full

[imgs]
index = none
tokenizer = none

[created]
type = date

[changed]
type = date
