project.name = article
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[id]
type = id

[uid]
tokenizer = full

[title]
type = title

[content]
type = body

[created]
type = date

[changed]
type = date

[status]
tokenizer = full

[locale_id]
tokenizer = full

[original_id]
tokenizer = full

[zcode]
tokenizer = full
